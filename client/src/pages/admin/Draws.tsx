import { PagePlaceholder } from "@/components/PagePlaceholder";
export default function AdminDraws() {
  return <PagePlaceholder title="Draws" note="Draw configuration, simulation, and publishing land with the draw-engine milestone." />;
}
