export default function AdminDashboard() {
  return (
    <div className="mx-auto max-w-6xl px-5 py-10 sm:px-8">
      <h1 className="text-3xl">Admin overview.</h1>
      <p className="mt-2 text-body">
        User management, draw control, charity content, winner verification, and reports all
        land in upcoming milestones. Auth and role-gating are live now — try visiting this page
        as a subscriber to confirm you're redirected.
      </p>
    </div>
  );
}
