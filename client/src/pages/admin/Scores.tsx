import { PagePlaceholder } from "@/components/PagePlaceholder";
export default function AdminScores() {
  return <PagePlaceholder title="Scores" note="Admin score editing lands with the score-management milestone." />;
}
